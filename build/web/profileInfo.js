
function displayReq(arr, done){
    if (done == false){
        getIndexForReq(userName, index, arr);
    }
    else if (done == true){
        //$("#testsubmission").html(arr.name + "<br />" + arr.credReq + "<br />" + arr.credApp);
        // DISPLAY ALL DIVS HERE
        $container = $("#profile");
        
        
        
        
        
    }
    
}